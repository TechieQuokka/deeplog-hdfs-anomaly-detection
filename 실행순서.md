● 🚀 실행 순서 가이드

  완벽합니다! 구현이 모두 완료되었으니 실행 순서를 알려드리겠습니다.

  ---
  📋 실행 순서

  Step 1: 환경 설정 확인

  # Python 버전 확인 (3.11 권장)
  python --version

  # 가상환경 생성 (선택사항이지만 권장)
  python3.11 -m venv venv
  source venv/bin/activate  # Windows: venv\Scripts\activate

  Step 2: 의존성 설치

  # requirements.txt로 패키지 설치
  pip install -r requirements.txt

  Step 3: 설정 확인 (선택)

  # 설정 출력해서 확인
  cd src && python config.py
  cd ..

  Step 4: 데이터 전처리

  # 데이터 전처리 실행 (첫 100k 로그로 train/test 분할, window 생성)
  python main.py --preprocess
  결과: data/processed/train_sequences.pkl, test_sequences.pkl 생성

  Step 5: 모델 학습

  # LSTM 모델 학습
  python main.py --train
  결과: checkpoints/best_model.pth 생성 (최고 성능 모델)

  Step 6: 모델 평가

  # Top-9 prediction으로 평가
  python main.py --evaluate
  결과: results/metrics.json 생성, 콘솔에 성능 출력

  ---
  ⚡ 한번에 실행 (All-in-One)

  # 전체 파이프라인 한번에 실행
  python main.py --all

  이 명령어가 자동으로:
  1. 데이터 전처리 → 2. 모델 학습 → 3. 평가 수행

  ---
  📊 예상 결과

  콘솔 출력 예시:

  ================================================================================
  Evaluation Results
  ================================================================================
  Total sessions: 570,204
  Abnormal sessions (ground truth): 16,838
  Predicted abnormal sessions: ...
  --------------------------------------------------------------------------------
  True Positives (TP):  ...
  False Positives (FP): 833  (목표)
  False Negatives (FN): 619  (목표)
  --------------------------------------------------------------------------------
  Precision: 0.9510 (95.10%)
  Recall:    0.9640 (96.40%)
  F1-Score:  0.9600 (96.00%)  ← 목표!
  ================================================================================

  ---
  🔍 개별 컴포넌트 테스트 (선택)

  전처리만 테스트:

  cd src && python preprocessing.py

  데이터셋 로딩 테스트:

  cd src && python dataset.py

  모델 아키텍처 확인:

  cd src && python model.py

  ---
  ⏱️ 예상 소요 시간

  - 전처리: ~5-10분 (데이터 크기에 따라)
  - 학습: ~30분-2시간 (GPU/CPU, 에폭 수에 따라)
  - 평가: ~5-10분

  총 소요 시간: 약 1-3시간

  ---
  🎯 다음 단계

  실행 후:
  # 결과 확인
  cat results/metrics.json

  # 로그 확인
  cat deeplog.log

  실행하시고 결과 알려주시면 논문 결과와 비교 분석해드리겠습니다! 🚀